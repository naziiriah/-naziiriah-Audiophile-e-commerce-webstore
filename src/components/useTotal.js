// import { useContext, } from "react";
// import { GearContext } from "../context/Context";

// const useTotal = ({ price, value }) => {
//     const [Items, purchase, Total, SetTotal] = useContext(GearContext)

    
    
    
//     return ( <>{SetTotal(Total + (price * value))}</> );
// }
 
// export default useTotal;
